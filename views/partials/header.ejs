<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="pt-10 font-['helvetica_now_display']">
    <nav class="w-full fixed top-0 left-0 z-50 bg-white shadow-md flex items-center justify-between px-6 py-4">
  <h3 class="text-2xl font-bold text-gray-800">Scatch</h3>

  
  <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
  <% if (loggedin) { %>
    <div class="flex items-center gap-6 text-gray-700 font-medium text-base">
      <a href="/shop" class="hover:text-black transition">Shop</a>
      <a href="/cart" class="hover:text-black transition">Cart</a>
      <a href="#" class="hover:text-black transition">My Account</a>
      <a href="/users/logout" class="text-red-600 hover:text-red-700 transition">Logout</a>
    </div>
  <% } %>
</nav>

  </body>
</html>
